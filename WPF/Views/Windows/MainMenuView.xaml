<UserControl x:Class="WPF.Views.Windows.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.Views.Windows"
             mc:Ignorable="d" 
             DataContext="{Binding MainMenuViewModel, Source={StaticResource ViewModelsLocator}}"
             d:DesignHeight="1000" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>

            <Setter Property="FontFamily" Value="Segoe UI"/>

            <Setter Property="FontSize" Value="18"/>

            <Setter Property="Padding" Value="15,10"/>

            <Setter Property="Margin" Value="10"/>

            <Setter Property="Background" Value="#FF007ACC"/>

            <Setter Property="Height" Value="100"/>

            <Setter Property="Width" Value="300"/>
        </Style>
    </UserControl.Resources>
    <Grid VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Rectangle Fill="White" Grid.Row="0"/>
        <Image Source="C:\Users\milko\source\repos\MyOwnGame\WPF\bin\Debug\game.jpg" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,20" Width="400"/>
        <Rectangle Fill="White" Grid.Row="1"/>
        <Grid Grid.Row="1" Margin="150">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Content="New Game" Command="{Binding MoveToNewGameCommand}" Grid.Row="0" Style="{StaticResource MenuButtonStyle}"/>
            <Button Content="Game Editor" Command="{Binding MoveToGameEditCommand}" Grid.Row="1" Style="{StaticResource MenuButtonStyle}"/>
            <Button Content="Exit" Command="{Binding CloseAppCommand}" Grid.Row="2" Style="{StaticResource MenuButtonStyle}"/>
        </Grid>
    </Grid>
</UserControl>