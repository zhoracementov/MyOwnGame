<UserControl x:Class="WPF.Views.Windows.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.Views.Windows"
             mc:Ignorable="d" 
             DataContext="{Binding MainMenuViewModel, Source={StaticResource ViewModelsLocator}}"
             Height="450" Width="800">
    <UserControl.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>

            <Setter Property="FontFamily" Value="Segoe UI"/>

            <Setter Property="FontSize" Value="18"/>

            <Setter Property="Padding" Value="15,10"/>

            <Setter Property="Margin" Value="10"/>

            <Setter Property="Background" Value="#FF007ACC"/>

            <Setter Property="Height" Value="100"/>

            <Setter Property="Width" Value="300"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Image Source="/Styles/game.jpg" Grid.Row="0" Stretch="Uniform"/>
        <Button Content="New Game" Command="{Binding MoveToNewGameCommand}" Grid.Row="1" Style="{StaticResource MenuButtonStyle}"/>
        <Button Content="Game Editor" Command="{Binding MoveToGameEditCommand}" Grid.Row="2" Style="{StaticResource MenuButtonStyle}"/>
        <Button Content="Exit" Command="{Binding CloseAppCommand}" Grid.Row="3" Style="{StaticResource MenuButtonStyle}"/>
    </Grid>
</UserControl>