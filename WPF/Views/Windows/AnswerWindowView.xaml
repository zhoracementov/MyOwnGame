<UserControl x:Class="WPF.Views.Windows.AnswerWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.Views.Windows"
             mc:Ignorable="d"
             DataContext="{Binding AnswerWindowViewModel, Source={StaticResource ViewModelsLocator}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Visibility="{Binding IsAvailable, Converter={StaticResource BoolToVisibilityConverter}}">
        <Border Background="Black" Opacity="0.6"/>
        <Border Margin="100" CornerRadius="10" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Grid.Column="0"
                           Grid.ColumnSpan="2"
                           Margin="10"
                           Text="{Binding TimeBefore, StringFormat=\{0:hh\\:mm\\:ss\}}"
                           FontSize="42"
                           Background="Red"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1"
                           Grid.Column="1"
                           Grid.ColumnSpan="2"
                           Grid.RowSpan="2"
                           Text="{Binding CurrentQuestionText}"
                           TextAlignment="Center"
                           Foreground="White"
                           Background="Black"/>
                <TextBox Grid.Row="3"
                         Grid.Column="1"
                         Grid.ColumnSpan="2"
                         FontSize="72"
                         Text="{Binding EnteredAnswerText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <!--<Button Grid.Row="2" Grid.Column="3"
                        Margin="10"
                        Content="Enter"
                        Command="{Binding CloseCommand}"/>-->
                <Button Grid.Row="3" Grid.Column="3"
                        Margin="10"
                        Content="Close"
                        Command="{Binding CloseCommand}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
