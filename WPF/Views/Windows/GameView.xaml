<UserControl x:Class="WPF.Views.Windows.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.Views.Windows"
             xmlns:v="clr-namespace:WPF.Views.Windows"
             mc:Ignorable="d"
             DataContext="{Binding GameViewModel, Source={StaticResource ViewModelsLocator}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <v:QuestionsTableView/>
        <Grid Visibility="{Binding IsGetAnswerWindowAvailable, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="Black" Opacity="0.6"/>
            <Border Margin="100" CornerRadius="10" Background="White">
                <Grid>
                    <TextBlock Text="{Binding TimeBefore}"
                           FontSize="72"
                           Background="Red"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
